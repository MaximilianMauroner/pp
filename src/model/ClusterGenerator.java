package src.model;

import src.controller.GameState;

import java.util.ArrayList;

public class ClusterGenerator {

    public static void generate(Entity test, Position position, int size, GameState gs) {
        // return test;
    }
    public static void generate(Ant test, Position position, int size, GameState gs) {
        // return test;
    }
    public static void generate(Hive test, Position position, int size, GameState gs) {
        // return test;
    }
    public static void generate(Obstacle test, Position position, int size, GameState gs) {
        // return test;
    }
    public static void generate(Food test, Position position, int size, GameState gs) {
        // return test;
//        for (int i =-(size/2); i < size/2; i++){
//            for (int j = -(size/2); j < size/2; j++) {
//                Position pos = new Position(position.getX() + i, position.getY() +j, gs.getStatus());
//                Point p = gs.getPoint(pos);
//                if(p != null){
//                    Point p = new Point(position,new ArrayList<>(test.clone()))
//                }
//            }
//        }
    }


}
