import java.util.function.Function;

public class IntensityUpdater implements Function<Intensity, Intensity> {
    @Override
    public Intensity apply(Intensity intensity) {
        return null;
    }
}
